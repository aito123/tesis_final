\thesistitle{$title$} % Your thesis title, this is used in the title and abstract, print it elsewhere with \ttitle
\supervisor{$thesis.supervisor.name$} % Your supervisor's name, this is used in the title page, print it elsewhere with \supname
\examiner{} % Your examiner's name, this is not currently used anywhere in the template, print it elsewhere with \examname
\degree{$thesis.degree-name$} % Your degree name, this is used in the title page and abstract, print it elsewhere with \degreename
\author{$author$} % Your name, this is used in the title page and abstract, print it elsewhere with \authorname
\addresses{} % Your address, this is not currently used anywhere in the template, print it elsewhere with \addressname

\subject{} % Your subject area, this is not currently used anywhere in the template, print it elsewhere with \subjectname

$if(thesis.keywords)$
\keywords{$thesis.keywords$} % Your keywords, this is used in the title page and abstract, print it elsewhere with \keywordname
$else$\keywords{}
$endif$

$if(thesis.city)$
\city{$thesis.city$} % Your city's name, this is used in the title page and abstract, print it elsewhere with \cityname
$else$\city{}
$endif$

$if(thesis.dateyear)$
\dateyear{$thesis.dateyear$} % Your year, this is used in the title page and abstract, print it elsewhere with \dateyearname
$else$\dateyear{}
$endif$

$if(thesis.university)$
\university{$thesis.university$} % Your university's name, this is used in the title page and abstract, print it elsewhere with \univname
$else$\university{}
$endif$

$if(thesis.department)$
\department{$thesis.department$} % Your department's name, this is used in the title page and abstract, print it elsewhere with \deptname
$else$\department{}
$endif$

$if(thesis.group)$
\group{$thesis.group$} % Your research group's name and URL, this is used in the title page, print it elsewhere with \groupname
$else$\group{}
$endif$

$if(thesis.faculty)$
\faculty{$thesis.faculty$} % Your faculty's name and URL, this is used in the title page and abstract, print it elsewhere with \facname
$else$
\faculty{}
$endif$

\setcounter{tocdepth}{$if(toc-depth)$$toc-depth$$else$3$endif$} % The depth to which the document sections are printed to the table of contents
